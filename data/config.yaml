jobs:
  directory: "{data-dir}/backups/{job-name}"
  configurations: "{data-dir}/config/*.json"
ftp:
  host: "0.0.0.0"
  port: 21
  user: "schiba"
  password: "Password1"
  directory: "{data-dir}/backups/"